<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="empScheduleDao">

	<update id="PjPostingUpdate">
		UPDATE	mpplace
		SET		cpid=apid
				, apid = null
				, cpstartday=apstartday
				, apstartday = null
				, cpendday = apendday
				, apendday = null
				, use_skill=ap_use_skill
				, ap_use_skill = null
		WHERE	apstartday=CURDATE()
	</update>

	<update id="PjPostingcpUpdate">
		UPDATE	mpplace
		SET		cpid=null
				, apid = null
				, cpstartday=null
				, apstartday = null
				, cpendday = null
				, apendday = null
				, use_skill=null
				, ap_use_skill = null
		WHERE	cpendday=CURDATE()
	</update>

	<update id="timeoff_Mpplace_Update">
		UPDATE	mpplace
		SET		cpid = null
				, cpstartday = null
				, cpendday = null
				, apid = null
				, apstartday = null
				, apendday = null
				, ap_use_skill = null
		WHERE	cpendday=(CURDATE()- interval 1 day )
		AND		cpid='휴직'
	</update>
	
	<update id="timeoff_employee_Update">
		UPDATE	employee
		SET		time_off='0'
				, to_start_day = null
				, to_end_day=null
		WHERE	to_end_day=(CURDATE()- interval 1 day )
		AND		time_off='1'
	</update>

</mapper>